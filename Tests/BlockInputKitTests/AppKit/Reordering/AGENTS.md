## Reordering Tests

- Cover both collection-view drag paths and leading reorder-handle starts when selection or chrome state can diverge.
- For ordered-list reorders, assert marker normalization through the model-reported changed blocks and the bounded large-document move path.
